# Optional. Overrides the database location. Without it, the daemon stores
# obsync.db in the platform data dir (~/.local/share/obsync on Linux,
# ~/Library/Application Support/obsync on macOS, %LOCALAPPDATA% on Windows).
#DB_FILE_NAME=file:local.db

# Optional. Overrides the API port (default 7274).
#PORT=7274
